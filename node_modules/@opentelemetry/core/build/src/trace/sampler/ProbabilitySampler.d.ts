import { Sampler, SpanContext, SamplingResult } from '@opentelemetry/api';
/** Sampler that samples a given fraction of traces. */
export declare class ProbabilitySampler implements Sampler {
    private readonly _probability;
    constructor(_probability?: number);
    shouldSample(parentContext?: SpanContext): SamplingResult;
    toString(): string;
    private _normalize;
}
//# sourceMappingURL=ProbabilitySampler.d.ts.map