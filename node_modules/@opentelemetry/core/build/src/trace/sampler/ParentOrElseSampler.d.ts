import { Sampler, SpanContext, SpanKind, Attributes, Link, SamplingResult } from '@opentelemetry/api';
/**
 * A composite sampler that either respects the parent span's sampling decision
 * or delegates to `delegateSampler` for root spans.
 */
export declare class ParentOrElseSampler implements Sampler {
    private _delegateSampler;
    constructor(_delegateSampler: Sampler);
    shouldSample(parentContext: SpanContext | undefined, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes, links: Link[]): SamplingResult;
    toString(): string;
}
//# sourceMappingURL=ParentOrElseSampler.d.ts.map